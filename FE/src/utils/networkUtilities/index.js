import { createBrowserHistory } from 'history';

// This is history was needed, so that we could access the history object outside of the
// components as well.
export const history = createBrowserHistory();
